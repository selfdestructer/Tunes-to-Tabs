# Scout.new Music-to-Tabs AI Setup

Create a Node.js application for converting music to guitar tabs using Google Lyria 2 and GCP.
Structure: backend (Express/TypeScript), frontend (React), infrastructure (GCP configs).
Focus on audio processing pipeline, tab generation, and user interface.
Use phased development approach as described in the CONTEXT.md file (provided separately in chat).

Key: Audio upload → Lyria 2 analysis → Guitar tab generation → Interactive viewer